# ==============================================================================
# NK411 — İnformasiya Təhlükəsizliyi Risk Reyestri
# Mühit Dəyişənləri (Environment Variables) Şablonu
#
# Bu faylı `.env` olaraq kopyalayın və production mühitinə uyğun doldurun.
# ==============================================================================

# ── 1. Server və Keş ─────────────────────────────────────────────────────────

NODE_ENV=development
PORT=3001
CORS_ORIGIN=http://localhost:5173

# Redis Konfiqurasiyası
REDIS_HOST=localhost
REDIS_PORT=6379

# ── 2. Verilənlər Bazası (PostgreSQL) ────────────────────────────────────────

DB_HOST=localhost
DB_PORT=5432
DB_NAME=risk_registry
DB_USER=riskadmin
DB_PASSWORD=changeme_in_production

# ── 3. Təhlükəsizlik (JWT) ───────────────────────────────────────────────────

# JWT Secret mütləq dəyişdirilməlidir! (Min 64 xarakter tövsiyə olunur)
# Yaradmaq üçün node konsolunda: require('crypto').randomBytes(64).toString('hex')
JWT_SECRET=changeme_very_long_secret_key_at_least_64_characters_for_production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=8h

# ── 4. LDAP / Active Directory (İsteğe Bağlı) ────────────────────────────────

LDAP_ENABLED=false
# LDAP_URL=ldap://your-dc.example.com:389
# LDAP_BASE_DN=DC=example,DC=com
# LDAP_BIND_DN=CN=svc-ldap,OU=ServiceAccounts,DC=example,DC=com
# LDAP_BIND_PASSWORD=changeme
# LDAP_GROUP_BASE_DN=OU=Groups,DC=example,DC=com
# LDAP_EMAIL_ATTR=mail
# LDAP_NAME_ATTR=displayName

# ── 5. Email Bildirişləri (SMTP) ─────────────────────────────────────────────

# SMTP serveri qeyd etmədikdə bildiriş sistemi avtomatik deaktiv olacaq
# Məsələn: GMail üçün smtp.gmail.com, port 587
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@risk-registry.az
